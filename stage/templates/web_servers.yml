# Generated by Terraform ${timestamp()}
# workspace name: ${workspace}
---
web_servers:
  - name: ${wordPress_hostname}
    ip: ${wordpress_ipv4_address}
  - name: ${gitLab_hostname} 
    ip: ${gitlab_ipv4_address}
  - name: ${grafana_hostname}
    ip: ${grafana_ipv4_address}
  - name: ${prometheus_hostname}
    ip: ${prometheus_ipv4_address}
  - name: ${alertmanager_hostname}
    ip: ${alertmanager_ipv4_address}
...